<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Osanpo-goo</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans+JP:400,700,900&display=swap" rel="stylesheet">
  </head>

  <body>
    <header class="header">
      <div class="inner">
        <div class="nav">
          <div class="nav__left">
            <%= link_to image_tag("logo.png", class: :logo1), root_path %>
              <%= link_to image_tag("logo1.png", class: :logo2), root_path %>
              <%= link_to image_tag("logo2.png", class: :logo3), root_path %>
          </div>
           <% if user_signed_in? %>
            <div class="nav__right">
              <%= link_to "ログアウト", destroy_user_session_path, class: :nav__logout, method: :delete %>
              <%= link_to "新規投稿", new_product_path, class: :nav__btn %>
            <div>
               <div class="nav__right2">
                 <%= form_with model: @product, local: true do |f|%>
                  <%#= form_with(url: search_products_path, local: true, method: :get, class: "search-form") do |form| %>
                    <%#= form.collection_select(:prefecture_id, Prefecture.all, :id, :name, {}, {class:"select-box1", id:"product-prefecture"}) %>
                    <%#= form.submit "検索", class: "search-btn" %>
                 <%# end %>
                  <%#= render partial: "product", locals: { product: products } %>
                  <%#= render partial: 'product', collection: @products %>
               </div>
              <% end %>
            <div>
          <% else %>
            <div class="nav__right">
              <%= link_to "ログイン", new_user_session_path, class: :nav__btn %>
              <%= link_to "新規登録",  new_user_registration_path, class: :nav__btn %>
            </div>
          <% end %>
        </div>
      </div>
    </header>
    <%= yield %>
    <footer class="footer">
      <p class="copyright"></p>
    </footer>
  </body>
</html>
